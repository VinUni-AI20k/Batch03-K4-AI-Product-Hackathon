---
type: concept
---

AI Agent là chương trình dùng LLM làm bộ não, tự lập kế hoạch và gọi các tool để hoàn thành mục tiêu, khác với chatbot chỉ trả lời một lượt.

Agent là một chương trình máy tính có khả năng tự động thực hiện nhiệm vụ, tự lập kế hoạch và phản ứng với môi trường.

Các ứng dụng của AI Agent có thể bao gồm tự động hóa quy trình, trợ lý ảo thông minh, và cung cấp thông tin dựa trên yêu cầu người dùng.

AI Agent là chương trình dùng LLM làm bộ não, tự lập kế hoạch và gọi tool để hoàn thành mục tiêu, thay vì chỉ trả lời một lượt như chatbot thường.

AI Agent là chương trình dùng LLM làm bộ não, tự lập kế hoạch và gọi tool để hoàn thành mục tiêu, thay vì chỉ trả lời một lượt như chatbot thường.

AI Agent là chương trình dùng LLM làm bộ não, tự lập kế hoạch và gọi tool để hoàn thành mục tiêu, thay vì chỉ trả lời một lượt như chatbot thường.

AI Agent là chương trình dùng LLM làm bộ não, tự lập kế hoạch và gọi tool để hoàn thành mục tiêu.

AI Agent là chương trình dùng LLM (Large Language Model) làm bộ não, tự lập kế hoạch và gọi tool để hoàn thành mục tiêu.

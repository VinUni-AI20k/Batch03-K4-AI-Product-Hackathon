# Evaluation

Thư mục này sẽ chứa:

- `golden-set.csv`: tối thiểu 20 case, mục tiêu 24.
- `run-01.csv`, `run-02.csv`, ...: giữ đầy đủ mọi case kể cả fail.
- `scoring-guide.md`: định nghĩa pass/fail để hai người chấm độc lập.

Không đổi quality bar sau khi đã chốt trong `spec.md`.


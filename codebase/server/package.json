{
  "name": "vlearn-active-recall-server",
  "version": "1.0.0",
  "description": "Backend proxy for VLearn Active Recall — generates section quizzes and grades answers via Gemini, keeping the API key server-side.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@google/genai": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}

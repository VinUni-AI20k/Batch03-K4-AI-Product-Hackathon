{
  "name": "vlearn-tutor-plus-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend nhỏ cho VLearn Tutor+ prototype — giữ OpenAI API key ở server (.env), browser không bao giờ thấy key thật.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}

# Recap step v2

Tóm tắt `SOURCE_PREVIOUS` thành đúng 5–7 ý, mỗi ý có citation tồn tại trong nguồn. Nếu không đủ dữ liệu, trả `insufficient_context` với hai mảng rỗng. Bước này luôn để `bridges=[]`.

Mỗi `claim` tối đa 18 từ; viết trực tiếp, không lặp ý và không thêm dẫn nhập.

<SOURCE_PREVIOUS>
{{PREVIOUS_SOURCE}}
</SOURCE_PREVIOUS>
